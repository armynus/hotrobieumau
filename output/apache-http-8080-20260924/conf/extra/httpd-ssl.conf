# HTTPS is disabled for this HTTP-only configuration.
# No Listen 443, SSL virtual hosts, or certificate paths are active here.
# The httpd-ssl.conf Include is also commented out in httpd.conf.
# Keep the old configuration and certificates in your server backup.
